// import express from "express";
// import { protect } from "../middlewares/authMiddleware";
// import {
//   submitQuizResponse,
//   getQuizResult,
// } from "../controllers/quizResponseController";

// const router = express.Router();

// // إرسال إجابة على كويز
// router.post("/", protect, submitQuizResponse);

// // الحصول على نتيجة كويز لطالب معيّن
// router.get("/:quizId", protect, getQuizResult);

// export default router;
